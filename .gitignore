# Documentación local — no subir a GitHub (pedido usuario)
documentacion/

# Notas internas de coordinación con agentes IA durante el desarrollo — no subir (mismo criterio que documentacion/)
.opencode/

# Node / pnpm — solo pnpm-lock.yaml es la fuente de verdad, sin package-lock.json (npm) duplicado
node_modules/
package-lock.json
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
lerna-debug.log*
logs
*.log

# Build Vite
dist/
dist-ssr/
*.local
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite / TS cache
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Dev logs
dev.log

# Supabase CLI (caché local de link/deploy, específico de esta máquina)
supabase/.temp/
supabase/.branches/
